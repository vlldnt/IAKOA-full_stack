# Développement uniquement (chargé par `npm run dev`, ignoré par le build de prod).
# URL relative : les requêtes passent par le proxy Vite (/api -> http://localhost:3000).
VITE_API_BASE_URL=/api
VITE_API_URL=/api
